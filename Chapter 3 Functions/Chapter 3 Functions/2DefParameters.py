#Aaron Stockman

#This goes over definition statements with parameters 

#stores a parameter(variable) inside the function, which is used when the function is called 
#in this example we have name as the parameter 
def hello(name):
    #this creates a function that adds hello to whatever we put the paramter as
    print('Hello ' + name)

#in this example we put Alice and Bob as the parameter name
hello('Alice')
hello('Bob')
